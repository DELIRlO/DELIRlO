name: WakaTime Stats Update

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'  # Executa ao meio-dia UTC (9h BR)

jobs:
  update-readme:
    name: Update WakaTime Stats
    runs-on: ubuntu-latest
    permissions:
      contents: write  # CRUCIAL para atualizar o README

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Importante para o histórico

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Run WakaTime Stats
        uses: anmol098/waka-readme-stats@v5.2
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          SHOW_TITLE: "false"
          SHOW_OS: "true"
          LOCALE: "pt"
          SHOW_LINES_OF_CODE: "true"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
